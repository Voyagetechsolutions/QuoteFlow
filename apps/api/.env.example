# Copy to .env and fill in.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/quoteflow?schema=public"
PORT=3000
WEB_ORIGIN="http://localhost:5173"

# Only needed for the vision extraction fallback (scanned sheets).
ANTHROPIC_API_KEY=""
